# Changelog

## 0.3.2 (2026-08-11)

- Upstream sync: bump pin `630eb9e` → `50e897d` (upstream v0.3.2). Nothing to port — the only mapped-path commit is the domain-directory reorganization (#513), which moved four mapped files with import-only diffs (`src/code-mode.ts` → `src/ingestion/code-mode.ts`, `src/ingestion.ts` → `src/ingestion/ingestion.ts`, `src/language.ts` → `src/platform/language.ts`, `src/constants.ts` → `src/config/constants.ts`; `src/agent/`, `src/okf/`, `src/mermaid/` stayed put). UPSTREAM.md's mapping table, out-of-scope list, and sync-procedure path list now use the new layout so future syncs don't miss moved files; provenance pointers across the skills and version stamps say v0.3.2. Out of scope: dependency pins (#616/#605), exec-bit build fix (#610), Windows MCP child env (#599), credentials/CLI module splits (#612/#611), error-classification hardening (#604), and upstream's own wiki updates.

- The two `references/index-labels.md` copies stay deliberately per-skill (skills install and load per-directory, so a shared sibling file would break standalone installs); UPSTREAM.md's sync procedure gains a drift guard — after any `index-labels.ts` sync, the two copies must diff empty.

- Progressive-disclosure pass over the wiki skills' SKILL.md files: the 38-row localized index-label table (upstream `index-labels.ts`) moves out of both wiki skills' Step 4 into per-skill `references/index-labels.md`, read only when the wiki's effective language is not English — the `en` default keeps its labels inline (`openwiki` 252→211 lines, `openwiki-personal` 466→425). Reviewed and deliberately kept inline: the personal skill's Step 3 prompt (every run executes it in full), `prompt-init.md`'s subagent prompts (the init run invokes them in the same run), the Step 2 translation rules and `.openwikiignore` semantics (small, skip-decision-bearing, and security-relevant) — splitting content a run reads anyway saves nothing.

## 0.3.1 (2026-08-05)

- Upstream sync: bump pin `9a02b35` → `630eb9e` (upstream v0.3.1). Link-validator false-positive fix (#585) ported into both wiki skills' Step 4 link pass: targets now resolve repo-wide instead of being confined to the wiki subtree (a wiki page may legitimately link out to any repository file; the `is outside the wiki root` verdict is gone — a link is broken only when its target genuinely does not exist, with `cannot be resolved` reserved for unresolvable paths), heading anchors are validated only on `.md` targets (directory anchors and GitHub `#L10` line anchors on source files are never flagged), and anchor slugs now match `github-slugger` exactly (Unicode combining marks kept; each whitespace character becomes its own hyphen instead of collapsing, so "A & B" slugs to `a--b`). Out of scope: telemetry fingerprinting (#589) and upstream's own wiki update (#583). Version stamps across skill intros, prompt references, and README now say v0.3.1.

- README restructured along upstream's layout (content unchanged, redistributed): a centered lockup header (`static/openwiki-skill-lockup.png` + tagline + badge, mirroring upstream's `static/openwiki-lockup.png` header), the feature-blob intro paragraph becomes a "gives you" highlight list, plus "What's new (v0.3.0)", "Quick start" (install → first prompt → keep current), and dedicated "Ignoring paths" sections; Install/Use fold into Quick start/Use.

## 0.3.0 (2026-08-04)

- Upstream sync: bump pin `08280e2` → `9a02b35` (upstream v0.3.0). The coding-agent prompt rewrite (#528) restructures both wiki skills: upstream replaced the shared prompt skeleton with per-command templates (`src/agent/prompts/code.ts`/`personal.ts`, rendered by a thin `prompt.ts` template engine), so `openwiki`'s Step 3 now routes to two new line-mapped files — `references/prompt-init.md` and `references/prompt-update.md` — while `openwiki-personal` keeps its single inlined Step 3 (upstream's personal init/update templates differ only in their "Mode-specific behavior" block).

- `openwiki` init is a new workflow (#528): repository mapping into a temporary `openwiki/_skeleton.md` (agent-deleted at the end — unlike `_plan.md` it is not runtime-removed and not snapshot-excluded), an evidence gate before any prose (entrypoint/implementation/types/persistence/callers/tests per component), no page budget (every substantial service/package/API gets a dedicated page or system directory), a task-routing table in quickstart, and a verification loop through three new init-only read-only subagents ported verbatim — `skeleton_critic` (independent repo audit of the skeleton, two-review protocol), `wiki_question_finder` (≤10 source-grounded questions with acceptance criteria), `wiki_answer_verifier` (wiki-only PASS/PARTIAL/FAIL batches) — from `src/agent/skeleton_critic.ts` + `src/agent/wiki_qa_subagents.ts`, with an `[adapted]` no-subagent fallback (perform the roles sequentially).

- `openwiki` update follows the new template (#528): the soft diff budget is gone ("Update every page needed... no preset limit"), new "Repository mapping discipline" and "Coding-agent utility requirements" sections (path compression, symbol-level Concept → Public API → Implementation → Tests mappings, change recipes, behavioral test matrices, quiet narrow validation commands), and the OKF front matter gains upstream's optional namespaced `openwiki` producer extension (roles/change_kinds/source_paths/symbols/test_paths/invariants/validation_commands).

- Runtime git evidence inverted (#528): upstream deleted the injected git summary (`createGitSummary` + its `.openwikiignore` filtering; `RunContext.gitSummary` dropped) — the update prompt now has the agent read `git rev-parse HEAD` / `git log <gitHead>..HEAD --name-status --oneline` itself, and with `.openwikiignore` active git history is simply unavailable ("rely on allowed source files and tests"). `openwiki` Step 1 shrinks to run context + the early no-op check (now with upstream's exact commands: `status --short --untracked-files=all`, `diff --name-only <gitHead>..HEAD`); the user prompts lose their injected git-context blocks and gain an `[adapted]` runtime-note explanation (`{RUNTIME_CONTEXT}`).

- Step 0 changes (#528, #547): the managed `AGENTS.md`/`CLAUDE.md` snippet body is rewritten upstream ("This repository has a generated `openwiki/` evidence index. It is optional just-in-time context, not required startup reading." plus two authority/validation bullets) — refreshing old-snippet repos is now a real write; and marker validation is strict — a file with markers must have exactly one ordered `START`/`END` pair, anything else fails setup with upstream's error before either file is written.

- Internal-link validation (#371) ported into both wiki skills: Step 4 becomes three deterministic passes (Mermaid → index sync → link validation from `src/agent/wiki-link-validator.ts`): inline links checked for file/directory existence and GitHub-style heading anchors (Unicode-preserving slugs, `-N` duplicate suffixes), wiki-root confinement, previous stamps cleared then broken links stamped in place with upstream's exact `openwiki: broken internal link` comment (never failing the run); both prompts gain the appended "Link integrity" section.

- `openwiki-personal` catches up to the v0.3.0 personal templates: new "Evidence discipline" (replaces the local-wiki "Git discipline"), revised Planning discipline (knowledge-domain inventory + revisit-after-drafting), rewritten Documentation goals / Section quality rules / Coverage self-check / Required-structure bullet, the cross-page linking bullet in OKF relationship modeling, a rewritten Mode-specific update block (canonical-page evidence mapping, "update every affected page", open-questions-first review), the init page budget removed, and the old shared-skeleton "Wiki-first question answering"/"Subagent discipline"/"Existing documentation discipline" sections dropped (wiki-first rules are chat-only upstream — `openwiki-ask`'s domain).

- automation.md CI templates fetch full git history (#577): GitHub Actions `fetch-depth: 0` (comment added; the setting itself was already port hardening), GitLab `GIT_DEPTH: "0"`, Bitbucket `clone: depth: full` — a shallow clone hides the last-documented commit and the update runs against an empty change summary.

- Out of scope in this sync: telemetry accuracy/diagnostics (#555/#576), env-save serialization (#549), tool-call argument rendering (#568, CLI UI), the agent graph factory refactor (#560), the changeset/README/deps chores, and upstream's own wiki updates. UPSTREAM.md remaps the prompt rows to the new template files, adds the subagent and link-validator rows, and widens the sync-procedure path list (now including `src/agent/prompts/`, the subagent files, `wiki-link-validator.ts`, `translation-middleware.ts`, `language.ts`, and `src/mermaid/`).

## 0.2.5 (2026-08-03)

- Upstream sync: bump pin `9c253af` → `08280e2` (upstream v0.2.5). `.openwikiignore` (#165) ported into `openwiki` (repository mode only — upstream gives local-wiki runs an inactive ruleset): Step 1 loads and parses the repo-root file (gitignore-compatible semantics: `*`/`**`/`?` globs, leading-`/` anchoring, trailing-`/` directory scoping, `!` negation with last match winning, case-insensitive matching over canonicalized paths so alternate spellings cannot dodge a rule) and filters every git-evidence section through it (a fully filtered section becomes upstream's "(all matching paths are excluded by .openwikiignore)" placeholder); the early no-op exit treats ignored-path changes like `openwiki/`-only changes, so an ignored path changing on its own never forces a rebuild; Step 3 gains upstream's ignore-conditional prompt variants (git-usage sentence, root-glob bullet, a replacement two-bullet "Git discipline") plus the ".openwikiignore discipline" section with the active pattern list — the backend's hard enforcement (ignored reads/edits denied, ls/glob/grep filtered, shell execute allowlisted to `pwd` / `git rev-parse HEAD` / `rm -f ./openwiki/_plan.md`; `src/agent/docs-only-backend.ts`) ported as `[adapted]` prompt discipline.

- Plan-file ownership follows upstream (#165): both wiki skills' Planning discipline now states the temporary plan is removed automatically after the run (do not treat it as a wiki concept or link to it) instead of telling the agent to delete it, the Coverage self-check drops its plan-deleted bullet, and Step 4's deletion is the removal path rather than a backstop.

- Out of scope in this sync: health-telemetry stage tagging (#500 — wraps mapped `migrateWikiToOkf`/`validateWikiMermaid`/`synchronizeWikiIndexes` calls in telemetry hooks without changing their behavior; this port has no telemetry runtime), changeset release tooling (#514, the `OPENWIKI_VERSION` re-export), comma-bearing gateway model ids (#530), and CLI/visualizer/connector plumbing. UPSTREAM.md carries next-sync notes for post-0.2.5 malformed-marker rejection (#547, Step 0 territory) and after-run internal-link validation (#371).

## 0.2.4 (2026-07-30)

- Upstream sync: bump pin `1a7c9fe` → `9c253af` (upstream v0.2.4). Multilingual output (#477) ported into both wiki skills: Step 1 resolves an effective wiki language (requested → persisted → explicit `en`; BCP-47 canonicalization with upstream's exact unrecognized-language warning), Step 2 gains the translation pass (`translation-middleware.ts` as an `[adapted]` you-are-the-model pass on update runs — translate-all only when a requested language's primary subtag differs from the persisted one, otherwise a pending-marker-only sweep; per-page failure stamps `openwiki_translation_pending: "<target>"` and never aborts) and normalization now stamps the localized derived `type` and carries `openwiki_translation_pending` across front-matter rebuilds (`PRESERVED_EXTENSION_FIELDS`), Step 3 gains the verbatim "Output language" section (the deterministic-pass line `[adapted]` to Step 2) and the ignore-the-translation-marker front-matter bullet, Step 4 localizes `Files`/`Directories` headings and the derived `type` via upstream's full `index-labels.ts` table (inlined; full tag → primary subtag → English resolution), Step 5 metadata gains `language`; Mode resolution routes explicit language requests as user instructions.

- Interrupted-run recovery (#365, #484 context) ported: `.last-update.json` gains `status: "complete" | "interrupted"` (absent = complete, so old metadata never forces a re-run); the `openwiki` early no-op exit refuses to skip when the previous run was interrupted; both skills' Step 5 failure path now writes `status: "interrupted"`, and a completed retry that changes no content still rewrites metadata once to clear a leftover interrupted status.

- `openwiki-personal`: contested claims (#453) ported — the confidence-label list gains `contested` and a "Contested knowledge discipline" block (preserve both claims in a `## Contested` section with source/date, label the fact contested everywhere including /themes.md Confidence cells, /open-questions.md entry only when future assistance is impaired, never resolve by recency alone); `references/sources.md`'s synthesis policy gains the same label and rule.

- New `openwiki` reference `references/runtime-evidence.md`: the LangSmith code-brains connector (#436, the v0.2.3 next-sync decision) ported as an on-request repo update run — the `langsmith` synthesis-guidance block verbatim (runtime complement to the static-code wiki: anomaly-weighted error/outlier/baseline sample, Observed/Correlated/Hypothesis registers, code-anchored checks, one consolidated `runtime-behavior.md`, privacy rules), with `[adapted]` evidence gathering (LangSmith MCP/API via host tools; the agent reproduces the deterministic pull's bucketed sampling) and the last-update ingestion window; Mode resolution and README route to it. The connector runtime itself stays out of scope.

- Out of scope in this sync: the trimmed-status-line no-op fix (#484 — a `runGit`-parsing bug in the CLI; this port reads `git status --short` directly, so there is nothing to port), GitHub Copilot provider (#192), Bedrock AWS-SDK credential chain (#418), chat checkpoint pruning (#476), base64 stream redaction (#215), connector-runtime HTTP resilience and config hardening (#417/#466/#401/#483/#402/#404), Windows test isolation (#467/#468), and CI/deps chores — CLI/provider/connector-runtime plumbing. UPSTREAM.md carries a new next-sync note for post-0.2.4 `.openwikiignore` (#165), which rewrites mapped prompt/utils/write-boundary code.

- Upstream sync: bump pin `0851444` → `1a7c9fe` (upstream v0.2.3). Mermaid diagrams (#199) ported into both wiki skills: Step 3 gains upstream's "Diagram discipline" block (embed source-grounded Mermaid diagrams where runtime flows, lifecycles, data models, or non-trivial control flow beat prose — sequenceDiagram/stateDiagram-v2/erDiagram/flowchart — few strong diagrams, one-line captions, degraded-fence repair rule) and the update-mode bullet making a missing diagram on a flow/lifecycle/data-model page a valuable improvement rather than a formatting-only change; Step 4 becomes "Validate diagrams, then synchronize directory indexes", porting `validateWikiMermaid` as an `[adapted]` self-run pass (upstream parses with optional `mermaid`+`jsdom` peers, falling back to a conservative heuristic — here the agent applies that heuristic plus the mermaid-diagrams syntax rules, fixes fences it can confidently repair, and otherwise degrades to a ```text fence behind upstream's exact "openwiki: mermaid parse failed" HTML comment); skill intros and Step 3 preambles now say v0.2.3 and name `src/mermaid/wiki.ts`.

- New skill `mermaid-diagrams`: upstream's bundled diagram-authoring skill, verbatim (diagram-type choices, grounding discipline, syntax-safety rules, update-run repair of degraded fences) — only the validation-ownership line carries an `[adapted]` marker (validation is the wiki skills' Step 4 pass here, not after-run code) and the front matter description is quoted. Both wiki skills' Diagram discipline points to it, README lists and installs it, and all three automation.md CI templates copy it alongside `openwiki`.

- `openwiki-personal`: ported #435 — "Root agent instruction files" gains two bullets: repository /AGENTS.md and /CLAUDE.md are instructions for repository code agents, and are not read or followed when inspecting a configured local repository as evidence unless the user explicitly asks about their contents.

- `openwiki`: Step 3's write-boundary `[adapted]` note records #406 (upstream's docs-only guard now normalizes `..`/`.` segments before the prefix check, so a path like `openwiki/../AGENTS.md` counts as outside — here: never write through a path that escapes openwiki/ after normalization); Step 0's `[omitted]` workflow note records #285 (since 0.2.3 only `openwiki code --init` creates the scheduled workflow and an existing file is never overwritten, so operator customizations survive — nothing to port, this skill never creates CI files).

- automation.md: the GitHub Actions template SHA-pins `actions/checkout` and `actions/setup-node` (upstream `ce8ce08` example hardening). Upstream's optional `mermaid`+`jsdom` CI installs are not mirrored: there is no CLI validator in this port — the agent runs Step 4's checks itself and uses a locally installed parser only when one happens to be available.

- Out of scope in this sync: provider base-URL env overrides (#430), OAuth-discovery hardening beyond the example pins, Windows OAuth-URL, launchd cron-combo, atomic `~/.openwiki/.env`, LangSmith-setup-reopen, and vertex-under-jsdom fixes — CLI/provider plumbing. Upstream's post-v0.2.3 LangSmith code-brains connector (#436) sits one commit past the pin; it touches mapped `src/ingestion.ts` with repo-wiki runtime-trace synthesis guidance, so UPSTREAM.md carries a next-sync note instead of a silent skip.

- Upstream sync: bump pin `264ee84` → `0851444` (upstream v0.2.2). Mapped-path changes are provider plumbing only (`constants.ts`: two Gemini model entries plus the `OPENWIKI_VERSION` bump); `prompt.ts`, `utils.ts`, `src/okf/`, `code-mode.ts`, `ingestion.ts`, `types.ts`, `skills/`, and `examples/` are unchanged — nothing to port. Skill intros and README now say v0.2.2; `log.md` generation still absent upstream (UPSTREAM.md watch note unchanged).

## 0.2.1 (2026-07-21)

- Upstream sync: bump pin `d4e94ab` → `264ee84` (upstream v0.2.1). OKF aligned with Google's v0.1 spec (#373/#376/#390): "Front matter requirements (OKF)" rewritten in both wiki skills — only `type` required (title/description/resource/tags now recommended-in-priority-order), `timestamp` and producer-defined extension fields accepted and preserved across round trips, `index.md`/`log.md` declared reserved documents, softer description/update-run wording; generated indexes lose their front matter (bundle-root index now carries only `okf_version: "0.1"`, per-directory title/description dropped, empty directories render `# Files`, `log.md` excluded from listings) — Step 4 re-ported from the renamed `src/okf/index-sync.ts`.

- Migration became a deterministic code pass (#390): upstream deleted the bundled `migrate-wiki-to-okf` skill, so this port deletes `skills/migrate-wiki-to-okf/` too. Its replacement is ported into both wiki skills as a Step 2 normalization block (before the work, from `migrateWikiToOkf`): a page whose front matter parses with a non-empty `type` is never touched; anything else gets a minimal derived block (`type: "Reference"`, `title` from first H1 or filename, `openwiki_generated: true`) for the run to enrich per the new prompt bullet ("OpenWiki repairs front matter deterministically after every run..."). Subagent discipline drops the per-directory migrate exceptions (upstream reverted them); Mode resolution now routes "migrate to OKF" requests to a plain update; README/automation.md drop the extra skill (README notes removing a v0.2.0-installed copy).

- Ported the new mode-parameterized prompt blocks (#383): repository runs gain their own "Canonical wiki location" block (the wiki is the repo-local openwiki/, never `~/.openwiki/wiki` — previously an `[omitted]` personal-only block) with `[adapted]` markers for the virtual-path prohibitions; the personal skill's canonical block and wiki-first bullets updated to the new upstream text; openwiki-ask notes upstream's repository/local wiki-first variants, which its two-wiki routing already embodies.

- Runtime bookkeeping (#394): both Step 2 snapshot commands exclude `_plan.md` (upstream's snapshot ignores it since 0.2.1, so a leftover plan never counts as content) and Step 4 deletes a leftover `openwiki/_plan.md` / `~/.openwiki/wiki/_plan.md` first (`removeTemporaryPlanFile` backstop).

- Out of scope in this sync: `--init` setup-wizard overhaul, default-brief proposal, runtime-root note, OAuth-URL/env-parsing/ChatGPT-token/gmail-config fixes, Windows ACL hardening, MCP env isolation, telemetry, and provider additions (Cloudflare `@`-prefixed model IDs, `OPENAI_BASE_URL`, OpenRouter pinning, provider-mismatch warning) — CLI/provider plumbing; UPSTREAM.md's OKF note updated (spec gaps closed upstream; `log.md` generation still absent — reserved-file handling only — keep watching).

## 0.2.0 (2026-07-17)

- Upstream sync: bump pin `7c084f9` → `d4e94ab` (upstream v0.2.0, "OKF + telemetry"). Ported into both wiki skills' Step 3: "Front matter requirements (OKF)" (Google Knowledge Catalog schema — the five-field formatter, retrieval-optimized descriptions, update-run preservation rule) with an `[adapted]` self-check standing in for upstream's write-time validator (`frontmatter-validator.ts`); "OKF relationship modeling" (concept nodes/edges, prose-anchored links, no reciprocal-link padding, ≥2 connections per substantive concept); planning discipline records source concept -> relationship meaning -> target concept; "Index discipline" plus a new Step 4 porting `index-middleware.ts` (deterministic per-directory `index.md` — `type: Documentation Index` front matter, sorted Files/Directories sections, title-cased directory names, write-only-on-diff), with metadata renumbered to Step 5; coverage self-check gains the concept-graph audit and the plan-deletion check; subagent discipline gains the migrate-wiki-to-okf exception (per-directory write-scoped subagents).

- New skill `migrate-wiki-to-okf`: upstream's bundled migration skill — add or correct OKF front matter across an existing wiki, one subagent per directory, bodies untouched; `[adapted]` wiki-root resolution (repo `openwiki/` vs `~/.openwiki/wiki`) and run wrapping in the matching wiki skill's Steps 2/4/5. Both wiki skills route to it from Mode resolution and from Step 4 when they meet a pre-OKF wiki; all three files state it is a one-time upgrade — init and 0.2.0 update runs write compliant front matter from the start, so a compliant wiki never routes there.

- Post-sync fidelity audit (three parallel line-level audits: both wiki skills diffed against the actually-rendered upstream prompt, plus the runtime wrappers and OKF machinery): fix `sources.md` still wrapping source updates in pre-renumber "Steps 1, 2, and 4" (now "Steps 1, 2, 4, and 5" — index sync was silently missing from source-update runs); make the Step 2 snapshot whitespace-safe (`-print0`/`sort -z`/`xargs -0`) and state the remaining upstream-hash deltas in an `[adapted]` note (upstream also hashes directory entries and scopes the metadata exclusion to the wiki root; comparison is within-run only); port `readLastUpdate`'s malformed-metadata-counts-as-none rule and the update-without-metadata "No prior OpenWiki update timestamp was found." git-context line; mark Step 0's identical-block write skip `[adapted]` (upstream rewrites unconditionally, byte outcome identical); document two rendering conventions in both Step 3 preambles (`docsLocation` shortened to `openwiki/` where upstream's raw template render doubles articles; "Mode-specific behavior — init/— update" headers standing in for upstream's single active-branch header). Everything else verified faithful, including every `index-middleware.ts` rendering detail and the byte-identity of the Step 0 snippet, migrate skill text, synthesis policy, and all seven connector guidance blocks.

- `openwiki-ask`: note OKF front matter descriptions (written for retrieval) and generated per-directory `index.md` files as grep/navigation targets.

- `openwiki` automation.md: CI templates also install `migrate-wiki-to-okf`; the GH Actions template gains upstream #370's fork opt-in guard (scheduled runs fire only in the origin repo or with the `OPENWIKI_ENABLE_SCHEDULED_UPDATE` repo variable; manual dispatch always allowed), generalized to an `OWNER/REPO` placeholder.

- Out of scope in this sync: telemetry (`src/telemetry/*`, the `telemetryFile` run option, the CI-example `OPENWIKI_TELEMETRY_DISABLED` note — no runtime to report on here); provider additions (Bedrock, Nebius, Vertex AI Claude, Gemini/Gemini Enterprise — provider plumbing in `constants.ts`); the DA skill-loading refactor (`skills.ts`, skill mounting — `migrate-wiki-to-okf` installs like any other skill here; the write-connector prompt line it removed has no counterpart in this port's `[adapted]` connector block); `log.md` (OKF spec §7 optional update-history file, mentioned in the 0.2 announcement post but absent from upstream code — upstream's index middleware would even throw on a spec-conformant front-matter-less `log.md`, so adding one here would break upstream-CLI interop; UPSTREAM.md records this and the other deliberate spec-subset choices to watch in future syncs).

- `openwiki-personal`: connectors.md Slack note — add the `search:read` scope family (matching upstream's `user_scope` in `src/auth/providers.ts`) for definitive self-message search via `search.messages`, with the bounded-history fallback behavior noted; note that Slack CLI login/service tokens (`xoxe.xoxp-…`) are app-management-only and cannot read conversations; tidy the guide — one connector table (GeekNews folded in as a port-only row), research-note phrasing dropped.

- `openwiki-personal`: align `[adapted]` lines with the connector guide — sources.md slack Evidence now covers the custom-app xoxp route (per-command `~/.openwiki/.env` load, `search:read` caveat), x Evidence mentions a local X CLI, "Other sources" points port-only feeds (geeknews) at the closest block, per-source intro links `connectors.md`; SKILL.md knowledge-source and security `[adapted]` lines now include authenticated CLIs.

- `openwiki-personal`: expand the `slack` row in `references/connectors.md` into a researched guide — custom internal app + xoxp user token route with the exact history scopes, why the May 2025 rate-limit reduction does not apply to internal custom apps, the official CLI's `slack api` passthrough, warnings against browser-session-token tools on managed workspaces, and the synthesis-not-mirror rule from Slack's API terms; document connector credential management in `~/.openwiki/.env` (upstream's own credential file, `src/env.ts` — upstream-style `OPENWIKI_*` names, loaded per command via `set -a; . ~/.openwiki/.env; set +a`, never read by the agent); add a gitignored `.internal/` directory for repo-internal research notes.

## 0.1.2 (2026-07-14)

- `openwiki-personal`: add `references/connectors.md` — guidance-only map of host-tool stand-ins per upstream connector, recording upstream's actual mechanism per source (direct OAuth APIs for Slack/Gmail/X, Tavily for web-search, public HN APIs, Notion MCP, local git-repo reads) alongside the suggested host tool (built-in web search/fetch, installed `git`, a local X CLI such as `birdclaw`, MCP servers or trusted CLIs for the rest); also records port-only sources — GeekNews via its official Atom feed (`news.hada.io/rss/news`; the site has no public JSON API); linked from the skill intro and README.

- Upstream sync: bump pin `326a307` → `7c084f9` (upstream v0.1.2). Ported: deferred-documentation-area tracking in both wiki skills' Step 3 (new "Coverage self-check" block, init records undocumented areas in a quickstart `## Backlog` section, update reads/promotes/prunes backlog entries); Step 4 now runs even when a run fails after generating content (`persistRunMetadataIfChanged`) so generated content stays diffable; automation.md gains a Bitbucket Pipelines template and the GitLab template's diff/add paths now include `AGENTS.md`/`CLAUDE.md` (upstream CI-example alignment; the workflow-file path is omitted — this port generates no CI files). Out of scope: CLI-reference/chat-mode rewording (`[omitted]` here), `OPENWIKI_PROVIDER` env pins and the Step-0 workflow template (provider/CI plumbing), OAuth/env/redaction fixes and new tests (CLI plumbing).

- README: catch up to the v0.1.1 sync — upstream port version 0.1.0 → 0.1.1, marker snippet now covers root `CLAUDE.md` as well as `AGENTS.md`, and document the `INSTRUCTIONS.md` "Wiki brief" in Use.

- Skills: bump stale upstream-version references (v0.1.0 → v0.1.1) in the `openwiki` and `openwiki-personal` SKILL.md intros and Step 3 reproduction notes, missed in the 0.1.1 sync.

## 0.1.1 (2026-07-11)

- Upstream sync: bump pin `bf8f84a` → `326a307` (upstream v0.1.1). Ported: "Wiki brief" block in both modes' init/update user prompts (repo brief read from `openwiki/INSTRUCTIONS.md`, personal from `~/.openwiki/INSTRUCTIONS.md`; the personal skill's `[adapted]` goal injection is now upstream-official); two new root-agent rules treating `openwiki/INSTRUCTIONS.md` as user-authored control metadata (read, never rewrite in routine runs); Step 0 now manages the marker snippet in `/CLAUDE.md` as well (`CODE_MODE_AGENT_FILES`), keeping the `[adapted]` @AGENTS.md-import-counts-as-covered exception; automation.md GH Actions template mirrors upstream (SHA-pinned create-pull-request action, add-paths includes AGENTS.md/CLAUDE.md). Out of scope: NVIDIA NIM provider, GPT-5.6 model options, ChatGPT OAuth fix, ephemeral checkpoints, issue templates (CLI plumbing).

## 0.1.0 (2026-07-10)

- Upstream 0.1.0 port: re-port `openwiki` to upstream's code mode (agent no longer edits AGENTS.md/CLAUDE.md mid-run — a new Step 0 manages the upstream `<!-- OPENWIKI:START/END -->` marker snippet, migrating legacy sections; write boundary "only under openwiki/" per upstream docs-only backend; GH Actions auto-creation intentionally omitted in favor of automation.md); add `openwiki-personal` (upstream personal/local-wiki mode: `~/.openwiki/wiki`, verbatim synthesis discipline — canonical pages, confidence labels, email taxonomy — metadata without gitHead, wiki goal via `~/.openwiki/INSTRUCTIONS.md`, per-source runs in `references/sources.md` with evidence from host MCP/web-search/local tools instead of OAuth connectors); `openwiki-ask` now covers both wikis with upstream's wiki-first rules; UPSTREAM.md remapped for 0.1.0 and pin bumped `a7c556f` → `bf8f84a`.

- README: add skills.sh badge.

- Upstream sync: bump pin `39493ef` → `a7c556f` (upstream v0.0.4). Only mapped-path change is the `OPENWIKI_VERSION` constant (0.0.2 → 0.0.4); the rest is CLI plumbing (non-TTY startup handling, tool-schema recovery middleware, stream-event simplification) with no skill counterpart — `src/agent/prompt.ts`, `src/agent/utils.ts`, and `examples/` unchanged, nothing to port.

- `openwiki-ask`: add ground rules ported from upstream's `chat` mode — answering is read-only (no `openwiki/` edits unless explicitly asked; hand off to the `openwiki` skill for init/update) plus the shared secret-handling rules (never read or quote secrets or `.env` files); UPSTREAM.md and the wiki now record `openwiki-ask` as a loose analogue of upstream `chat` instead of "no upstream counterpart".

- Upstream sync: bump pin `7d35537` → `39493ef` (upstream v0.0.2). Mapped-path changes since the pin are CLI-only (`OPENWIKI_VERSION` bump, `OPENROUTER_FALLBACK_MODEL_IDS` removal — provider plumbing with no skill counterpart); `src/agent/prompt.ts`, `src/agent/utils.ts`, and `examples/` are unchanged, so nothing to port.

- `openwiki`: when neither root instruction file exists, also create `CLAUDE.md` containing `@AGENTS.md` (Claude Code reads only CLAUDE.md; the import is its documented pattern), and treat an importing CLAUDE.md as semantically current on update runs.

- Initial release: `openwiki` skill (verbatim port of the langchain-ai/openwiki agent prompt @ `7d35537`, wrapped in the CLI's runtime steps — git evidence with early no-op, content-snapshot hash, metadata write) and `openwiki-ask` skill (wiki-first Q&A), plus a keyless automation guide with scoped permissions and CI templates.
